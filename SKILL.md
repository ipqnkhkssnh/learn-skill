---
name: learn-skill
description: 学习技能——把用户提供的录屏/操作视频抽帧后交给视觉模型逐帧学习，理解"前置条件是什么、每一步的目的是什么"，并把一次具体操作泛化成可复用的系统/页面/任务知识，落成新的 skill 存到 ~/.agent/skills；之后执行该技能时遇到没学过的分支（例如只录过查询订单、这次要提交订单），可以边操作边补学、自动回写优化该技能。触发词：学习技能、学一下这个录屏、从录屏里学、看录屏学操作、抽帧学习、录屏教学、把这个操作变成技能、生成技能、技能补学、更新技能、优化技能、learn from recording、skill from video。
whenToUse: 用户给出录屏/视频/截图序列并要求"学会"其中操作；或要求把一套手工流程固化成技能；或正在执行某个已学技能、当前需求超出技能已有内容，需要边做边补学并回写。
---

# 学习技能 · learn-skill

> 一句话：**把"人怎么点"变成"系统是什么、能做什么、怎么做"**，落成可复用技能；用的时候缺什么就补学什么，并回写。

## 0. 先判定模式（不要混着做）

| | **模式 A · 从录屏学习** | **模式 B · 执行中补学回写** |
|---|---|---|
| 触发 | 用户给了录屏/视频/截图，要求"学一下" | 正在执行某个已学技能，当前需求超出技能已有内容 |
| 输入 | 视频文件或目录 | 正在使用的技能包 + 现场真实操作 |
| 产物 | 新建技能包（或要求时重建） | 增量更新该技能包 + CHANGELOG |
| 入口 | 本文 §2 | 本文 §7 |

两种模式共用同一套理解方法与落盘格式（§3–§6）。

## 1. 铁律（违反任何一条，本次学习作废）

1. **不臆造**：技能里每一句关于界面的描述，必须有帧证据或现场实测证据。推断出来的标 `confidence: inferred`；没看到的写进 `unknowns`，宁缺毋假。
2. **学能力，不学路径**：目标是"ERP 订单模块怎么用"，不是"怎么查订单号 12345"。任何具体值都必须参数化。
3. **前置条件写在最前面**：录屏第一帧之前就已经成立的（已登录、已在某页面、某条数据已存在），必须单独成节，不许混进步骤里。
4. **每步都要有目的**：写不出目的的步骤，说明还没看懂——回到帧里重看。
5. **凭据不入库**：账号、密码、token、cookie、身份证、手机号、真实客户数据一律不写进技能包。只写"凭据来源"（如"由用户提供"）。
6. **破坏性操作只登记、不默认执行**：提交/审批/删除/支付/发货/群发这类操作，录屏里看到就记录入口、前置条件、需要填什么；缺失的标 `unknown`。除非用户明确授权本次实操。
7. **先只读后写入**，写操作前先确认环境是测试还是生产。
8. **一步一验证**：在操作通道上每做一步先截图确认，再走下一步。禁止盲连击。
9. **技能包保持干净**：抽帧缓存、原始录屏不放技能包内，放 `~/.agent/skills/.learn-cache/` 或临时目录。

## 2. 模式 A 标准流程

### Step 0 · 定位录屏并体检

```bash
VIDEO="/path/to/recording.mov"     # 用户给的位置；若是目录，列出文件让用户选
ls -lh "$VIDEO"
```

确认三件事：文件可读、时长多少、**录屏是否包含登录/进入系统的过程**（这决定"入口"知识要靠推断还是靠观察）。

### Step 1 · 抽帧（JPEG）

```bash
SKILL_DIR="${LEARN_SKILL_DIR:-$HOME/.agent/skills/learn-skill}"
OUT="$HOME/.agent/skills/.learn-cache/$(date +%Y%m%d-%H%M%S)-$(basename "${VIDEO%.*}")"
bash "$SKILL_DIR/scripts/extract_frames.sh" "$VIDEO" "$OUT" \
     --interval 1 --max 300 --width 1280 --base64 --batch-size 20
```

- 时长 < 60s → `--interval 0.5`；> 10min → `--interval 3`~`5`。决策表见 `references/frame-sampling.md`。
- 表单输入 / 快速点击密集的片段 → 二次加密：`--start 62 --end 78 --interval 0.25`。
- 表格、小字、密集字段看不清 → 加 `--ocr on`，脚本会输出逐帧识别文字，与图片互为补充。
- 产出：`frames/*.jpg`（按画面相似度去重）+ `manifest.json`（序号/时间戳/尺寸/差异值/OCR）+ `index.md` + `batches/*.json`（JPEG 的 base64 批次）。

帧数控制在 **60–300 张/遍**。超过就分段学，不要一次喂几百张。

### Step 2 · 看图，做时间轴笔记

**投喂方式就是 `JPEG → base64 → 多模态模型`：**

- **首选**：对 `frames/frame_00001.jpg` 直接调用运行时的看图能力（如 `read_image`）——运行时内部会做 JPEG→base64 编码并把图交给模型，你不用操心。
- **显式批次**：需要换模型/换 provider 喂图、脚本化调用、或运行时没有看图能力时，用 `batches/batch_XXXX.json` 里的 `images[].base64`（即 `data:image/jpeg;base64,<...>`），连同 `index`/`tLabel`/`ocr` 作为文字上下文一起发。

分批读，每批 20–40 张，**严格按 index 顺序**。每批看完先写一段笔记再读下一批：

```
[f012 00:12] 左侧菜单 "订单管理" 高亮 → 进入订单列表页。顶栏有搜索框、日期筛选、"新建订单"按钮。
[f018 00:18] 在搜索框输入 10 位数字（疑似订单号），点"查询"。列表从 3 页收敛到 1 行。
[f024 00:24] 点首行"详情" → 订单详情页：状态=待发货、金额、收货人、商品明细表。
[f031 00:31] 鼠标滚轮下滚，出现"物流信息"与"操作日志"两个折叠区。
```

要点：
- **看不清的帧不要跳过**——二次抽帧放大该时间段（Step 1 的 `--start/--end`），必要时 `--ocr on` 交叉验证。
- 每批笔记里必须区分：**看到的**（`observed`）与**猜的**（`inferred`）。
- 记录鼠标位置/焦点、输入框内容、按钮文字、页面标题、面包屑——这些是"页面有什么"的原始素材。

### Step 3 · 归纳理解（学习的核心，别省）

按 `references/analysis-playbook.md` 的三层模型逐层提炼：

- **L1 系统层**：这是什么系统、入口是什么（URL/IP/客户端/桌面图标/门户路径）、怎么登录、账号从哪来、导航骨架长什么样。
- **L2 页面层**：每个页面 = 到达路径 + 用途 + 区域结构 + 关键元素（字段、按钮、表格列、筛选器、分页、状态标签）+ 可见反馈 + 副作用。
- **L3 任务层**：目标 → 前置条件 → 步骤（参数化）→ 每步预期反馈 → 成功判据 → 异常分支 → 副作用。

**前置条件必须四查**：身份权限（登录/角色）、环境（URL/网络/VPN/客户端版本）、数据（要操作的对象是否已存在、什么状态）、时序（必须先完成的动作）。

**目的识别**：用"如果省略这一步会怎样"反问自己。答不上来就标推测，不要写成事实。

### Step 4 · 泛化（决定这个技能有没有价值）

| 录屏里看到的 | 必须泛化成 |
|---|---|
| 订单号 `SO20240912-0087` | 参数 `${order_no}`，并写清参数从哪来（用户提供 / 上游查询结果） |
| 查了一个订单 | "按条件查询订单：订单号 / 客户 / 日期区间 / 状态" |
| 走了一条菜单路径 | 该模块的**完整入口地图**（所有可见的进入方式，不止走的那条） |
| 点了"详情" | 页面提供的**全部能力**：查看 / 新建 / 编辑 / 删除 / 导出 / 打印 / 审批（区分"录屏验证过"与"只在界面上看到"） |
| 界面上有但没点的按钮 | `unknowns` 清单，标 `needs-exploration`，留给模式 B 补学 |

**反例 vs 正例**（用户真实场景）：

- ❌ 错误学法：`erp查订单技能 = 登录ERP → 点订单管理 → 输入 SO20240912-0087 → 点详情 → 滚轮下滚`
- ✅ 正确学法：`ERP 订单管理技能 = { 入口: ${ERP_URL} + 登录前置(凭据来源) ; 页面: 订单列表(筛选字段/列/分页/批量操作)、订单详情(字段分区/操作按钮) ; 任务: 按条件查订单(参数化)、查看订单详情、[未知] 提交订单(入口已见,待补学) }`

### Step 5 · 落盘技能包

```bash
bash "$SKILL_DIR/scripts/init_skill.sh" erp-order-management \
  --system "ERP" --title "ERP 订单管理"
```

生成到 `~/.agent/skills/erp-order-management/`，目录与字段规范见 `references/skill-format.md`。然后把 Step 3/4 的结论填进 `SKILL.md`、`pages/*.md`、`tasks/*.md`、`meta.json`。

**技能名必须是小写 kebab-case ASCII**（如 `erp-order-management`），中文只出现在标题和正文里。

### Step 6 · 自检 + 汇报

```bash
bash "$SKILL_DIR/scripts/validate_skill.sh" ~/.agent/skills/erp-order-management
```

自检清单：
- [ ] 前置条件独立成节，且说清了"录屏前就已成立"的那部分
- [ ] 每个任务都参数化，没有硬编码具体值
- [ ] 每条界面描述都有帧序号或实测证据支撑
- [ ] `meta.json` 的 `unknowns` 如实列出（尤其界面上看到但没操作过的功能）
- [ ] 无凭据、无真实客户数据
- [ ] 技能名 kebab-case，frontmatter 有 `name` + `description`

汇报给用户时给三样：**学到了什么（能力清单）**、**没学到什么（unknowns）**、**建议下一步补学什么**。

## 3. 操作通道选择（学习、补学、执行都遵守）

优先级（先探测再决定，不要凭印象）：

| 优先级 | 通道 | 何时用 | 判定 |
|---|---|---|---|
| 1 | **`remote-a2desk`** | 默认首选：远程 Linux VM + 虚拟桌面，隔离、不打扰用户本机、可快照重放 | 工具列表里存在 `mcp__remote-a2desk__*` |
| 2 | **`local-a2desk`** | 远程不可用/目标系统只能在本地访问 | 存在 `mcp__local-a2desk__*` |
| 3 | **Playwright MCP（无头浏览器）** | 目标在浏览器里、且必须在本机操作 | 存在 `mcp__playwright__*`（或同类命名） |

**"首选远程"不是"无脑远程"**：远程不可用时（MCP 未配置、连接失败、截图黑屏/超时、远程访问不到目标系统（内网/VPN）、远程 VM 没起）要**明确说明降级原因**，再切本机。

**本机桌面通道会抢占用户的鼠标键盘**——开始前先告知用户，操作期间不要离开，做完立刻释放。

工具的坐标语义、截图→点击的正确姿势、Playwright 的定位策略，见 `references/execution-channels.md`。三条通用纪律：

1. **操作前先截图**，确认当前状态符合该步的前置条件；不满足就先补齐（如先登录）。
2. **操作后再截图**，确认反馈符合预期；不符合就走异常分支。
3. **每步留证**：远程/本地操作的关键节点截图存到技能包 `assets/` 或缓存目录，作为技能的实测证据。

## 4. 技能包规格（速览）

```
~/.agent/skills/<skill-name>/
├── SKILL.md          # frontmatter(name/description/whenToUse) + 系统入口 + 前置条件 + 能力清单 + 任务索引
├── pages/            # 页面知识：每页一个文件，写"有什么、能做什么、怎么到达"
├── tasks/            # 任务配方：每个任务一个文件，参数化步骤 + 验证点 + 异常分支
├── state/meta.json   # 机器可读元数据：版本、来源录屏、覆盖度、unknowns、使用统计
├── assets/           # 关键截图证据（可选，压缩过）
└── CHANGELOG.md      # 每次补学/修正都追加一条
```

完整规范、模板、合并/回写算法见 `references/skill-format.md`。

> **路径别名**：DSH 扫描的用户技能根是 `~/.agents/skills`（`$DSH_AGENTS_HOME/skills`）。`~/.agent` 是指向 `~/.agents` 的符号链接，因此 `~/.agent/skills` 与 `~/.agents/skills` 是同一个目录。写入前先确认别名存在：
> ```bash
> [ -e "$HOME/.agent" ] || ln -s "$HOME/.agents" "$HOME/.agent"
> ```

## 5. 学习质量的红线

- **不要把录屏当成 UI 录屏**：录屏里的界面是 ERP，但技能要覆盖的是"订单管理"这个能力域，包括没录到但界面上存在的功能。
- **不要用"点击坐标"当知识**：坐标会随分辨率/窗口变化而失效。优先用**语义定位**（菜单名、按钮文字、字段标签）；只有确实没有语义时（自绘界面、Canvas、游戏类）才记坐标，并注明分辨率。
- **不要把"看到"写成"能操作"**：界面上的按钮不等于你有权限用、也不等于你知道点击后填什么。没验证过的标注清楚。
- **不要静默跳过长录屏里看不懂的片段**：标注 `unclear @ 时间点`，并在汇报里告诉用户，请他补充说明。

## 6. 参考文档

| 文件 | 什么时候读 |
|---|---|
| `references/analysis-playbook.md` | Step 3/4：怎么从帧里读出前置条件、目的，怎么泛化（含完整 ERP 示例） |
| `references/frame-sampling.md` | Step 1/2：采样密度决策、成本与 token 估算、OCR、二次加密、JPEG→base64 投喂批次 |
| `references/execution-channels.md` | 需要操作桌面/浏览器时：a2desk 与 Playwright 的正确用法 |
| `references/skill-format.md` | Step 5/7：技能包格式、模板、版本与回写合并算法 |

## 7. 模式 B · 执行中自动优化（自学习闭环）

**触发条件**（满足任一即进入本模式）：

- 执行技能时发现目标功能在技能里只有入口、没有步骤（例：录屏只学过"查订单"，这次要"提交订单"）；
- 实际界面与技能描述不符（字段名变了、按钮位置变了、多了新按钮）；
- 用户提出了技能没覆盖的新参数/新场景（新查询条件、批量导出、审批流）。

**流程：**

1. **读取现状**：把正在使用的技能包完整读一遍，列出**本次要用但技能里没有**的点，写成待补清单。
2. **先确认安全边界**：涉及写操作（提交/删除/审批）时，先向用户确认可在当前环境实操。未获授权 → 只补"只读"部分，其余留在 `unknowns`。
3. **边操作边学习**：按 §3 的通道优先级选通道，**每步先截图 → 操作 → 再截图**，同步记录：
   - 实际点了什么（语义定位，不是坐标）；
   - 需要填什么（字段名、必填性、格式、校验报错原文）；
   - 系统反馈（成功提示、状态变化、跳转目标）；
   - 该操作的**前置条件**（权限、数据状态）与**副作用**（库存/状态/下游单据变化）。
4. **一键回写**：操作跑通后**立即**（不要等下一个任务）更新技能包：
   - `tasks/` 新增或修正任务配方（参数化）；
   - `pages/` 补充新发现的页面/区域/按钮；
   - `SKILL.md` 更新能力清单与任务索引；
   - `state/meta.json`：`version` +0.1、`updatedAt`、从 `unknowns` 移除已验证项、`usageCount`/`lastUsedAt`；
   - `CHANGELOG.md` 追加一条：日期 / 触发场景 / 新增或纠正了什么 / 证据（截图文件名）/ 仍未解决什么；
   - 顺手更新 `learn-skill` 自己的 `state/registry.json` 里对应条目（版本/unknowns 数量/最后使用时间）。
5. **矛盾以实测为准**：与技能旧描述冲突时，改旧描述并在 CHANGELOG 标 `纠正`，不要两份说法并存。
6. **不要动的技能**：若技能包来自他人共享或 git 仓库（`state/meta.json` 的 `origin` 非 `learned-local`），不要直接改，产出补丁/副本并告知用户。
7. **校验**：`validate_skill.sh` 跑一遍；把 "补学了什么、还差什么" 汇报给用户。

**并行安全**：同一技能可能被并行使用——回写前重读文件，只做增量合并，不要整文件覆盖。

---

**记住：这个技能的产物不是一份操作手册，而是一份"系统地图 + 能力清单"**——后面用户提任何相关问题，都应该能靠它直接上手，而不是重新看录屏。
