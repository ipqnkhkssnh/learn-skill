# tasks/ · 任务配方

每个任务一个文件，命名用 kebab-case 动词短语（如 `query-order.md`、`submit-order.md`）。
**任务必须参数化，且每一步都要写清"为什么做"和"做完看什么"。**

复制本文件的骨架新建任务：

---

# 任务：<任务名>

## 目的

<用户做这件事想达成什么。写不出目的说明还没看懂——回到录屏重看。>

## 前置条件

- **身份 / 权限**：<需要的角色、菜单权限、数据权限>
- **环境**：<URL / 系统版本 / 是否需 VPN>
- **数据**：<需要什么数据、处于什么状态才能做>
- **时序**：<必须先完成的前置动作>

## 参数

| 参数 | 含义 | 来源 | 示例 | 必填 |
|---|---|---|---|---|
| `${order_no}` | 订单号 | 用户提供 / 上游查询结果 | SO20240912-0087 | 是 |

## 步骤

| # | 操作（语义定位） | 输入 / 参数 | 预期反馈（怎么知道这步成了） | 失败怎么办 |
|---|---|---|---|---|
| 1 | 左侧菜单点「订单管理」 | — | 出现订单列表页，标题为"订单管理" | 菜单不存在 → 检查权限 |
| 2 | 顶部搜索框输入订单号，点「查询」 | `${order_no}` | 列表只剩 1 行且订单号匹配 | 无结果 → 确认订单号与环境 |

## 成功判据

<可观察、可断言的结果。例：列表首行状态列显示"待发货"。>

## 副作用

<会不会改数据、触发下游单据、发通知。写"无"也要显式写。>

## 异常分支

| 现象 | 原因 | 处理 |
|---|---|---|

## 证据

- 帧 f018–f024（00:18–00:24）；现场实测截图 `assets/xxx.jpg`

## 置信度与未知

- 已验证：<哪几步是实测/录屏确证的>
- 未验证：<哪些是推断，或界面上看到但没点过的>
